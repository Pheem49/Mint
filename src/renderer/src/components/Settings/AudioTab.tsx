// Re-export from the shared module — do NOT add logic here.
// Edit src/renderer/shared/components/Settings/AudioTab.tsx instead.
export { default } from '../../../shared/components/Settings/AudioTab'

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Capabilities for Mint desktop windows managed by the Rust backend.",
  "windows": ["main", "settings", "spotlight", "widget", "screen-picker"],
  "permissions": ["core:default"]
}
